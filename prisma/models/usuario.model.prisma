model Usuario {
  id          Int           @id @default(autoincrement())
  email       String        @unique
  nome        String?
  tipo        TipoUsuario        @default(USER)
  ListaCompra ListaCompra[]
}
